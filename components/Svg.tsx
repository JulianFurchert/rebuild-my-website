import { styled } from '../stitches.config'
import { motion } from "framer-motion"

export const Svg = styled('svg', {
  fill: 'currentColor'
})

export const Path = styled(motion.path, {
  fill: 'currentColor'
})

export const Circle = styled(motion.circle, {
  fill: 'currentColor'
})

export const Rect = styled(motion.rect, {
  fill: 'currentColor'
})

export const G = styled(motion.g, {
  fill: 'currentColor'
})